export default function ComplaintList({ complaints, filters, onFilterChange, onEdit, onDelete, loading }) {
  return <section className="px-6 pb-20"><div className="mx-auto max-w-7xl overflow-hidden rounded-[2rem] bg-white p-8 shadow-lg"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-moss">Your reports</p><h2 className="mt-1 font-display text-3xl font-bold text-forest">Complaint List</h2></div><span className="rounded-full bg-mint px-4 py-2 text-sm font-bold text-forest">{complaints.length} {complaints.length === 1 ? 'report' : 'reports'}</span></div><div className="mt-6 grid gap-3 rounded-2xl bg-sand/60 p-4 md:grid-cols-2"><label className="text-sm font-semibold text-forest">Waste Type<select value={filters.wasteType} onChange={(e) => onFilterChange('wasteType', e.target.value)} className="mt-1 block w-full rounded-lg border bg-white p-2"><option value="">All waste types</option>{['Organic Waste', 'Plastic Waste', 'E-Waste', 'Hazardous Waste'].map((type) => <option key={type}>{type}</option>)}</select></label><label className="text-sm font-semibold text-forest">Status<select value={filters.status} onChange={(e) => onFilterChange('status', e.target.value)} className="mt-1 block w-full rounded-lg border bg-white p-2"><option value="">All statuses</option>{['Pending', 'In Progress', 'Resolved'].map((status) => <option key={status}>{status}</option>)}</select></label></div>{loading ? <p className="mt-6 text-forest/70">Loading complaints...</p> : !complaints.length ? <div className="mt-6 rounded-2xl border border-dashed border-forest/20 bg-sand/40 p-8 text-center text-forest/70">No complaints match the selected filters.</div> : <div className="mt-6 overflow-x-auto"><table className="complaint-table w-full text-left text-sm"><thead className="border-b bg-sand/60 text-forest"><tr>{['Citizen', 'Location', 'Waste Type', 'Priority', 'Status', 'Description', 'Action'].map((heading) => <th key={heading} className="p-3">{heading}</th>)}</tr></thead><tbody>{complaints.map((complaint) => <tr key={complaint._id} className="border-b border-forest/10"><td className="p-3 font-semibold text-forest">{complaint.citizenName}</td><td className="p-3">{complaint.location}</td><td className="p-3">{complaint.wasteType}</td><td className="p-3">{complaint.priority}</td><td className="p-3"><span className="rounded-full bg-mint px-2.5 py-1 text-xs font-bold text-forest">{complaint.status}</span></td><td className="max-w-xs p-3 text-forest/70">{complaint.description}</td><td className="space-x-2 whitespace-nowrap p-3"><button onClick={() => onEdit(complaint)} className="rounded-lg bg-mint px-3 py-1.5 font-semibold text-moss">Edit</button><button onClick={() => onDelete(complaint._id)} className="rounded-lg bg-red-50 px-3 py-1.5 font-semibold text-red-600">Delete</button></td></tr>)}</tbody></table></div>}</div></section>
}
